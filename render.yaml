services:
  - type: web
    name: ned-sams
    env: python
    rootDir: server
    buildCommand: bash ./render-build.sh
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.8"
      - key: UPLOAD_FOLDER
        value: /var/data/student_documents
      - key: DATABASE
        value: /var/data/database.db
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        sync: false
    disk:
      name: ned-sams-data
      mountPath: /var/data
      sizeGB: 1

